#This file lists environment variables for the application. It is used by the development environment and can also be used in production if you choose to load it there. It should not contain secrets, but rather configuration values that are safe to share. For example, you might include a variable for the application version or a flag to enable debug mode.
APP_VERSION=0.1.0
